// App configuration settings
export const APP_CONFIG = {
  // Set to true to use video intro, false for original logo animation
  USE_VIDEO_INTRO: true,
  
  // Video intro duration (milliseconds)
  VIDEO_INTRO_DURATION: 8500,
  
  // Fallback intro duration (milliseconds)
  FALLBACK_INTRO_DURATION: 3000,
};